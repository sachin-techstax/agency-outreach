SERPER_API_KEY=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
YOUR_NAME="Sachin Rajan"
PORTFOLIO_URL=
CALENDLY_URL=
MIN_SCORE=70
DISCOVERY_LIMIT=15
FOLLOWUP_DAYS=4
# Host/local-Python defaults. Docker Compose overrides these with /data and
# /secrets paths inside the containers.
DB_PATH=nuntago.db
GMAIL_CLIENT_SECRET=client_secret.json
GMAIL_TOKEN_FILE=token.json
LOG_LEVEL=INFO
LOG_FILE=

# Nuntago operator UI. Demo mode serves fictional data and blocks every
# persistent/external action (pipeline runs, Gmail drafts, status mutations).
NUNTAGO_DEMO_MODE=false

# Nuntago application authentication.
# Private production should enable this and set a high-entropy bearer token.
# The token is entered into the operator UI at runtime and stored only in the
# browser tab's sessionStorage. Never compile it into the frontend bundle.
NUNTAGO_AUTH_ENABLED=false
NUNTAGO_API_TOKEN=

# Optional Docker file-ownership overrides for Linux hosts.
LOCAL_UID=1000
LOCAL_GID=1000
